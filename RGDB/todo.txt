GetEffectPropName(num, idx:integer):string;
var
  i:integer;
begin
  result:=ReturnText('SELECT value'+IntToStr(i)+' FROM effects WHERE id='+IntToStr(num));
end;


The string_agg(X,Y) function is an alias for group_concat(X,Y).


Фильтр по модам:
база, список, всё


inventory - добавить косметические слоты

filter by mod[list]
check for mod[list]

Add modid filter (global)
  get info from base [first line], return mod[s]
  get info from base for selected mod[s]

area (entrance coord) - сложность в том, что у карты могут быть 2-3 модификации. т.е координаты разные

effects:
  number(alphabetically)
  name

??GUI для ввода-редактирования данных вручную
  Mods
  Items
  Props
  Classes
  Skills
  Pets
  Mobs
  Quests
  Stats
  Recipes
