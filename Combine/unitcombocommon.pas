unit unitComboCommon;

{$mode ObjFPC}{$H+}

interface


type
  tact = (
    ask,
    stop,
    skip,
    skipall,
    compare,
    overwrite,
    overwritedir,
    overwriteall, // for binaries only?
    renameold,
    saveas
  );


implementation

end.

