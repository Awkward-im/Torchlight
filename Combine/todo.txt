
SynEdit1.ExecuteCommand(ecEditorBottom, '',nil);
SynEdit1.InsertTextAtCaret('texto insertado');
------------
procedure TForm1.SynEdit1Scroll(Sender: TObject; ScrollBar: TScrollBarKind);
begin
   SynEdit2.TopLine := SynEdit1.TopLine;
end;
------------------
// Подготавливаем SynEdit-ы к синхронной прокрутке
procedure TForm1.FormCreate(Sender: TObject);
begin
   SynEdit1.OnStatusChange := SynEditStatusChange;
   SynEdit1.Tag := 1;
   SynEdit2.OnStatusChange := SynEditStatusChange;
   SynEdit2.Tag := 2;
end;

// Это - приватный метод класса формы, который и будет прокручивать
// один SynEdit при прокрутке пользователем другого:
procedure TForm1.SynEditStatusChange(Sender: TObject;
                 Changes: TSynStatusChanges);
var Syn : TSynEdit;
begin
   if TSynStatusChange.scTopLine in Changes then
   // Как изменилось состояние? Изменился ли индекс верхней строки?
   begin
      Syn := Sender as TSynEdit;
      case Syn.Tag of
         1 : SynEdit2.TopLine := Syn.TopLine; // Тогда другому компоненту выставить такой же индекс
         2 : SynEdit1.TopLine := Syn.TopLine;
      end;
   end;
end;
-------------------------

а - оставить как есть (отменить изменения)
б - заменить старое новым
в - обновить старое
  пересканировать
  "скопировать справа налево"
  "удалить слева"
  "следующая разница"
  "предыдущая разница"

Удаление пресета
копирование текущего мода сначала в отдельную папку
запись протокола действий как сценария (с прописыванием замены, пропуска)

1 - папка назначения
2 - папка исходного мода
3 - (параметры замены) - в моде
4 - (текущая папка для ForceDirectory) - в моде
5 - Лог

skip *.BIN* files?

save as bin  - faster, compiled mods, but editable just DAT files + imagesets
save as text - easier to compare but can't compile LAYOUT
as source

build list of available mods
make pack list
  1st  - as is
  next - 
     not exists - add
     exists:
        same - skip
        not the same - ask for action (skip, replace; modify - for text files only)
        text files: DAT, LAYOUT, IMAGESET, ANIMATION
        (overwrite/skip for another files? once, for this mod)

statistic:
  mod: total files, existing, overlapped (diff content)

>src dirs, dst dir, dst mod header

Action: skip/overwrite file, dir, all, modify file
=========
Compare text files:
show diff (skip spaces), copy right to left, edit left, delete line, insert line
---
если папка, составить сортированный список,
если есть бинарный и текстовый файлы, сравнить дату (брать бинарный по возможности)
как вариант, делать это только для layout, остальные нормально переводятся
если файл найден:
  1 - перезаписать
  2 - пропустить
  3 - сравнить, и 1 или 2. или отредактировать.
      годится для DAT, но не LAYOUT (нет компиляции и обработки бинарных данных)
Запрос:
  перезаписать
  пропустить
  (для всех файлов мода)
  (только для двоичных файлов)